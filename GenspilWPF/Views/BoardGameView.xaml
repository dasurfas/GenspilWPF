<UserControl x:Class="GenspilWPF.Views.BoardGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:model="clr-namespace:GenspilWPF.Models"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenspilWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Row 1 (soegning) -->
            <RowDefinition Height="Auto"/>
            <!-- Row 2 (toolbar) -->
            <RowDefinition Height="Auto"/>
            <!-- Row 3 (main body) -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Soegefelter -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Height="60">

            <!-- Titel felt -->
            <StackPanel Orientation="Vertical" Margin="8">
                <TextBlock Text="Titel"/>
                <TextBox Width="130" Height="26" Text="{Binding SearchTitle}" Tag="Søg titel...">
                </TextBox>
            </StackPanel>
            
            <!-- Genre -->
            <StackPanel Orientation="Vertical" Margin="8">
                <TextBlock Text="Genre"/>
                <TextBox Width="130" Height="26" Text="{Binding SearchGenre}" Tag="Fx Strategi..."/>
            </StackPanel>
            
            <!-- Stand -->
            <StackPanel Orientation="Vertical" Margin="8">
                <TextBlock Text="Stand"/>
                <ComboBox
                    x:Name="ConditionComboBox"
                    Width="130"
                    Height="25"
                    SelectionChanged="ConditionComboBox_SelectionChanged"
                    SelectedIndex="0"
                    />
            </StackPanel>

            <!-- Antal Spillere -->
            <StackPanel Orientation="Vertical" Margin="8">
                <TextBlock Text="Spillere"/>
                <TextBox Width="130" Height="26" Text="{Binding SearchMinPlayers}" Tag="Fx 2 eller 3-4..."/>
            </StackPanel>

            <!-- Pris -->
            <StackPanel Orientation="Vertical" Margin="8">
                <TextBlock Text="Maks pris"/>
                <TextBox Width="130" Height="26" Text="{Binding SearchMaxPrice}" Tag="Kr..."/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="15,24,0,0">
                <Button Content="Søg" Width="40" Command="{Binding SearchCommand}"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Toolbar -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="8,5,5,5" Content="+Tilføj spil" Command="{Binding AddBoardGameCommand}"></Button>
            <Button Grid.Column="2" Margin="5,5,15,5" Content="Udskriv liste" IsEnabled="False"></Button> <!-- TODO -->
        </Grid>
        
        <!-- Data Grid -->
        <Grid Grid.Row="2">
            <DataGrid
                ItemsSource="{Binding BoardGames}"
                SelectedItem="{Binding SelectedBoardGame}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                >
                <DataGrid.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditBoardGameCommand}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Titel" Width="130" Binding="{Binding Title}"/>
                    <DataGridTextColumn Header="Genre" Width="130" Binding="{Binding Genre}"/>
                    <DataGridTextColumn Header="Stand" Width="100" Binding="{Binding GameCondition}"/>
                    <DataGridTextColumn Header="Spillere" Width="80" Binding="{Binding PlayerRange}"/>
                    <DataGridTextColumn Header="Pris" Width="80" Binding="{Binding Price}"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,2" Margin="0,3" Opacity="0.75">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#085041"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="På_Lager">
                                                    <Setter Property="Background" Value="#085041"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="Ikke_På_Lager">
                                                    <Setter Property="Background" Value="#b70000"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="Reserveret">
                                                    <Setter Property="Background" Value="#0C447C"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="TilReparation">
                                                    <Setter Property="Background" Value="#6f0070"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="PåVej">
                                                    <Setter Property="Background" Value="#bea500"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GameStatus}" Value="Solgt">
                                                    <Setter Property="Background" Value="#444444"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding GameStatusText}" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Noter" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Text="{Binding Notes}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip="{Binding Notes}"
                                    ToolTipService.InitialShowDelay="10"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Rediger" Margin="5,0" Command="{Binding DataContext.EditBoardGameCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Slet" Margin="5,0" Command="{Binding DataContext.DeleteBoardGameCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
