<UserControl x:Class="GenspilWPF.Views.ReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenspilWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Button Content="Ny Forespørgsel" Margin="8,5,5,5" HorizontalAlignment="Left" Command="{Binding AddReservationCommand}"/>
        </Grid>
        <Grid Grid.Row="2">
            <DataGrid
                ItemsSource="{Binding Reservations}"
                SelectedItem="{Binding SelectedReservation}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                >
                <DataGrid.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditReservationCommand}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Titel" Width="120" Binding="{Binding GameTitle}"/>
                    <DataGridTextColumn Header="Kundenavn" Width="120" Binding="{Binding CustomerName}"/>
                    <DataGridTextColumn Header="Kontakt" Width="120" Binding="{Binding ContactInfo}"/>
                    <DataGridTextColumn Header="Dato" Width="120" Binding="{Binding Date}"/>
                    <DataGridTextColumn Header="Status" Width="100" Binding="{Binding Status}"/>
                    <DataGridTemplateColumn Header="Noter" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Text="{Binding Notes}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip="{Binding Notes}"
                                    ToolTipService.InitialShowDelay="10"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Rediger" Margin="5,0" Command="{Binding DataContext.EditReservationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Slet" Margin="5,0" Command="{Binding DataContext.DeleteReservationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
