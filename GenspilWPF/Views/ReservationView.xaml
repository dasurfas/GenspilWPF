<UserControl x:Class="GenspilWPF.Views.ReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenspilWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Button Content="Ny Forespørgsel" Margin="8,8" Width="100" HorizontalAlignment="Left" Command="{Binding AddReservationCommand}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Titel" Margin="8,0" Width="120"/>
            <TextBlock Text="Kundenavn" Margin="8,0" Width="120"/>
            <TextBlock Text="Kontakt" Margin="8,0" Width="120"/>
            <TextBlock Text="Dato" Margin="8,0" Width="120"/>
            <TextBlock Text="Status" Margin="8,0" Width="120"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <DataGrid
                ItemsSource="{Binding Reservations}"
                SelectedItem="{Binding SelectedReservation}"
                AutoGenerateColumns="False"
                >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Titel" Width="120" Binding="{Binding GameTitle}"/>
                    <DataGridTextColumn Header="Kundenavn" Width="120" Binding="{Binding CustomerName}"/>
                    <DataGridTextColumn Header="Kontakt" Width="120" Binding="{Binding ContactInfo}"/>
                    <DataGridTextColumn Header="Dato" Width="120" Binding="{Binding Date}"/>
                    <DataGridTextColumn Header="Status" Width="120" Binding="{Binding Status}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
